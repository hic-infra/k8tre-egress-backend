apiVersion: v1
kind: Service
metadata:
  name: {{ include "hic-egress-backend.fullname" . }}
  labels:
    {{- include "hic-egress-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hic-egress-backend.selectorLabels" . | nindent 4 }}
