apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hic-egress-backend.fullname" . }}
  labels:
    {{- include "hic-egress-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "hic-egress-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "hic-egress-backend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "hic-egress-backend.fullname" . }}-config
            - secretRef:
                name: {{ include "hic-egress-backend.fullname" . }}-secret
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
