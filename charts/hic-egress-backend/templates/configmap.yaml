apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hic-egress-backend.fullname" . }}-config
  labels:
    {{- include "hic-egress-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
